import { LeadConflictModel } from './lead-conflict-model';

describe('LeadConflictModel', () => {
  it('should create an instance', () => {
    expect(new LeadConflictModel()).toBeTruthy();
  });
});
