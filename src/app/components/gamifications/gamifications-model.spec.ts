import { GamificationsModel } from './gamifications-model';

describe('GamificationsModel', () => {
  it('should create an instance', () => {
    expect(new GamificationsModel()).toBeTruthy();
  });
});
