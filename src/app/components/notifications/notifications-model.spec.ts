import { NotificationsModel } from './notifications-model';

describe('NotificationsModel', () => {
  it('should create an instance', () => {
    expect(new NotificationsModel()).toBeTruthy();
  });
});
