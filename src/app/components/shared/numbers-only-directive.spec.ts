import { NumbersOnlyDirective } from './numbers-only-directive';

describe('NumbersOnlyDirective', () => {
  it('should create an instance', () => {
    expect(new NumbersOnlyDirective()).toBeTruthy();
  });
});
